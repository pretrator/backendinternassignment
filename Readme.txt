This Software needs nodejs installed in the computer. Please do ensure before executing
Use the below given series of command to execute the project
1. clone the repo
2. open terminal in the root folder
Provide the given command in series to install
3. npm i
4. npm i --prefix client

Give the below command to execute the project
5. npm run dev

All commands are to be gien in root folder